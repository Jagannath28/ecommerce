server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/jobdb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

jwt:
  secret: changeThisSecretToAStrongOne123456
  expiration-ms: 86400000
